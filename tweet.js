import { text } from "body-parser";
import { log } from "console";

const tweet = prompt("Enter is your tweet?");
const tweetCount = tweet.length;
alert(
  "You have inserted " +
    tweetCount +
    " texts and you have " +
    (240 - tweetCount) +
    " texts remaining."
    if (tweetCount >= 240) {
        alert("You have no inputs.");
    }
    else {
        alert("")
    }
);
